apiVersion: split.smi-spec.io/v1alpha1
kind: TrafficSplit
metadata:
  name: traffic-split
  namespace: linkerd-project
spec:
  service: api-service
  backends:
  - service: error-injector
    weight: 900m
  - service: api-service
    weight: 100m